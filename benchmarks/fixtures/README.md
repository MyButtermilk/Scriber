# Fixtures

Live Microsoft and Soniox fixtures are intentionally not faked. Add
`de-short.wav` and `de-medium.wav` only when their transcript oracles can be
stored under `benchmarks/oracles/` and the files are approved for benchmark use.
