# Adversarial plan/target repair v5.02

Use only the immutable post-style-v5.01 plans and targets, the blinded v5
adversarial reviews with their private case index, the assigned contracts, and
the independently audited scope counts. Do not read training, validation,
test, challenge, AI-Gold, model-output, or judge data. Do not call a generative
API and do not use human curation.

Apply only closed, evidence-backed transformations:

- bind each reviewed feedback-list date to the due date of its obligation fact;
- mark each reviewed uncertain date as not yet confirmed in both plan and list;
- preserve the declared semantic role `Rückmeldung zur Position ...`;
- integrate remaining `Die Regelung gilt ...` condition commentary through the
  closed condition-realization helper;
- remove the exact completed-check restatement only after its closed
  equivalence proof;
- naturalize the reviewed English technical compound while preserving the
  exact term;
- turn the exact one-item `Inhalt bleibt unverändert.` list into a paragraph
  and set the plan list kind to `none`;
- restore every literal `fiktiv*`/`Fictional` qualifier present in fact text in
  its natural grammatical case, without inferring qualifiers from metadata;
- localize the complete declared structural vocabulary of the exact reviewed
  `de-DE` scope while preserving names, references, URLs, and embedded
  technical terms;
- replace each adjudicated syntactic pronoun misreference by repeating the
  actor name already present in that sentence and remove only the displaced
  pronoun from that fact's protected values.

The fictionality, German structural-language, and pronoun scopes were each
confirmed as genuine by a third independent adjudication and are therefore
active repairs. Keep all `en`, `mixed`, and code-switching hard-negative
language cases outside the exact `de-DE` scope unchanged.

Preserve every fact, entity, protected value, date, number, URL, role, modality,
polarity, and order except for the named structural corrections. Validate the
seed schema, canonical record, AST/SST/renderers, protected spans, batch
contract, seven canonical variants per target, pinned review/index/source
hashes, exact scope and overlap counts, and two byte-identical builds.
